<div class="container">
    {{#each artistsList}}
    <div class="item-wrapper">
        <img src="{{images.0.url}}" style="height: 200px;">
        <p>{{name}}</p>
        <a href="/albums/{{id}}">View Albums</a>
    </div>
    {{/each}}
</div>